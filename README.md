# Mini CRUD Python + SQLite

Revisão rápida de Python, SQL e CRUD.

## Como rodar
```bash
python -m venv .venv
# Ative o venv (Windows: .venv\Scripts\activate | Linux/Mac: source .venv/bin/activate)
pip install -r requirements.txt
python app/main.py